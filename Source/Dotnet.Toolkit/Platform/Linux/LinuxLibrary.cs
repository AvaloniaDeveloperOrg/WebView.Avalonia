namespace Dotnet.Toolkit.Platform.Linux;

public class LinuxLibrary
{
    public const string so = nameof(so);

    public const string libdl = nameof(libdl);
    public const string libdl2Library = $"{libdl}.{so}.2";



}
